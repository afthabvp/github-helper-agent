services:
  - type: web
    name: github-helper-agent
    runtime: docker
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: AGENT_AUTH_TOKEN
        sync: false
    healthCheckPath: /health
